import { PartialRemarkOptions } from 'remark';
import { Processor } from 'unified';
import { cleanCurly, isShortcodeAST, ShortcodeAST } from './utils';
export interface ShortcodeOptions {
    /**
     * Array of shortcode tokens as `[start, end]`
     *
     * @default ```
     * [ ['{{<', '>}}'], ['{{%', '%}}'] ]
     * ```
     */
    tokens?: Array<[string, string]>;
    /**
     * Whether to parse inline shortcodes
     *
     * @default false
     */
    inlineMode?: boolean;
    /**
     * Attributes where value contains markdown
     *
     * @default []
     */
    markdownAttributes?: string[];
}
export declare function shortcodes(this: Processor<PartialRemarkOptions>, options?: ShortcodeOptions): void;
export { cleanCurly, isShortcodeAST, ShortcodeAST };
